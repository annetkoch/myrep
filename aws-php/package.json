{
  "name": "@TestJS-example/aws-php",
  "version": "0.0.0",
  "dependencies": {
    "@TestJS/aws-s3": "workspace:*",
    "@TestJS/core": "workspace:*",
    "@TestJS/dashboard": "workspace:*",
    "TestJS": "workspace:*"
  },
  "devDependencies": {
    "esbuild": "^0.17.1"
  },
  "private": true,
  "type": "module",
  "scripts": {
    "start": "php -S localhost:8080 serve.php",
    "outputBundle": "esbuild --format=esm --sourcemap=inline --bundle ./main.js"
  }
}
